# Only consulted if a recipe execs BusyBox init. The default /init hands off to
# /recipe-init instead, so this is here for interactive/rescue use.
::sysinit:/bin/mount -t proc proc /proc
::sysinit:/bin/mount -t sysfs sysfs /sys
::sysinit:/bin/mount -t devtmpfs devtmpfs /dev
::respawn:-/bin/sh
